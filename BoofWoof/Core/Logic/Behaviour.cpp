#include "pch.h"

/*

#include "Behaviour.h"
#include "../ECS/Coordinator.hpp"
#include "LogicSystem.h"

Behaviour::Behaviour(const char* behaviourName, const InitBehaviour& init, const UpdateBehaviour& update, const DestroyBehaviour& destroy)
	: mBehaviourName(behaviourName), InitFunction(init), UpdateFunction(update), DestroyFunction(destroy)
{
	//g_Coordinator.GetSystem<LogicSystem>()->AddBehaviour(this);
}

void Behaviour::Init(Entity entity)
{
	InitFunction(entity);
}

void Behaviour::Update(Entity entity)
{
	UpdateFunction(entity);
}

void Behaviour::Destroy(Entity entity)
{
	DestroyFunction(entity);
}
*/